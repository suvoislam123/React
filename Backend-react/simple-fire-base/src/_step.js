/**
 * Steps to use firebase
 * 1.create a project on console.firebase.google.com
 * 2.install firebase sdk (npm install firebase)
 * 3.Register web app in firebase
 * copy firebase init with config from firebase project setting(firebase.config.init)
 * export default app
 * import app firebase init.js into your app.js
 * import {getAuth} from 'firebase/auth' and create const auth = getAuth(app)
 * turn on firebase authentication
 * create google auth provider{only for google authentication}
 * Then next....code.....
 */